<?xml version="1.0" encoding="utf-8"?>
<!-- Ornate gold band: dim at the edges, bright polished gold in the centre. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="0"
        android:startColor="@color/gold_dim"
        android:centerColor="@color/gold_light"
        android:endColor="@color/gold_dim" />
</shape>
