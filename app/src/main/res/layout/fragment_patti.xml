<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_ancient">

    <!-- Title -->
    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="பாட்டி பொம்மை"
        android:textSize="19sp"
        android:textColor="@color/gold_light"
        android:fontFamily="serif"
        android:letterSpacing="0.05"
        android:shadowColor="@color/bg_edge"
        android:visibility="gone"
        android:shadowDx="0"
        android:shadowDy="2"
        android:shadowRadius="4"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="10dp" />

    <TextView
        android:id="@+id/subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Patti Wobble Doll"
        android:textSize="13sp"
        android:textColor="@color/parchment"
        android:letterSpacing="0.18"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Ornament divider -->
    <View
        android:id="@+id/rule"
        android:layout_width="150dp"
        android:layout_height="2dp"
        android:background="@drawable/title_ornament"
        android:visibility="gone"
        android:layout_marginTop="6dp"
        app:layout_constraintTop_toBottomOf="@id/subtitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Patti wobble view in a framed niche — single image pendulum -->
    <com.thanjavur.wobbledoll.PattiWobbleView
        android:id="@+id/pattiView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/panel_niche"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintTop_toBottomOf="@id/rule"
        app:layout_constraintBottom_toTopOf="@id/hint"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginHorizontal="4dp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="12dp" />

    <!-- Hint pill -->
    <TextView
        android:id="@+id/hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tap the doll  •  Shake your phone"
        android:textSize="12sp"
        android:textColor="@color/parchment"
        android:letterSpacing="0.10"
        android:background="@drawable/hint_pill"
        android:paddingHorizontal="18dp"
        android:paddingVertical="7dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="12dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
